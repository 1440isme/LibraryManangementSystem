<Page
    x:Class="ThuVienApp.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"

    Background="#F8FAFC">

    <Grid>
        


        <!-- Main Content -->
        <StackPanel Grid.Column="1" Margin="20">
            <TextBlock FontSize="24" FontWeight="Bold" Margin="0,0,0,20">Hello Evano 👋</TextBlock>

            <!-- (Giữ nguyên phần stats hiện tại của bạn...) -->

            <TextBlock Text="All Students" FontWeight="Bold" FontSize="18" Margin="0,10,0,2"/>
            <TextBlock Text="Active Members" Foreground="#5C22FF" FontSize="14" Margin="0,0,0,10"/>

            <toolkit:DataGrid x:Name="StudentsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="350" HeadersVisibility="Column">
                <toolkit:DataGrid.Columns>
                    <toolkit:DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="*" />
                    <toolkit:DataGridTextColumn Header="Book" Binding="{Binding Book}" Width="*" />
                    <toolkit:DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="SizeToCells" />
                    <toolkit:DataGridTextColumn Header="In Time" Binding="{Binding InTime}" Width="SizeToCells" />
                    <toolkit:DataGridTextColumn Header="Out Time" Binding="{Binding OutTime}" Width="SizeToCells" />
                    <toolkit:DataGridTemplateColumn Header="Status" Width="SizeToCells">
                        <toolkit:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="5" Padding="5" HorizontalAlignment="Center"
                                        Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}">
                                    <TextBlock Text="{Binding Status}" Foreground="White" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </toolkit:DataGridTemplateColumn.CellTemplate>
                    </toolkit:DataGridTemplateColumn>
                </toolkit:DataGrid.Columns>
            </toolkit:DataGrid>
        </StackPanel>
    </Grid>
</Page>
